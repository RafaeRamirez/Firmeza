@model string
@{ ViewData["Title"] = "Firmeza Assistant"; }
<h2>Firmeza Assistant (AI Studio)</h2>

<div asp-validation-summary="All" class="text-danger"></div>

<form method="post" asp-action="Index" asp-controller="Chat">
  <div>
    <label for="userMessage">Pregunta sobre el negocio</label>
    <textarea id="userMessage" name="userMessage" rows="3" placeholder="Ej: Recomiéndame 3 productos para un cliente que busca X"></textarea>
  </div>
  <button type="submit">Preguntar</button>
</form>

@if (!string.IsNullOrWhiteSpace(Model))
{
  <hr />
  <h4>Respuesta</h4>
  <div>@Model</div>
}